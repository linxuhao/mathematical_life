# Prediction 1 Experiment — Python Dependencies
# =============================================
# Install with: pip install -r requirements.txt
#
# For AMD ROCm PyTorch, install torch separately first:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm6.0

# ── Core ───────────────────────────────────────────────────────────
numpy>=1.24
scipy>=1.10
tqdm>=4.65

# ── ML / Transformers ──────────────────────────────────────────────
transformers>=4.40
datasets>=2.18
accelerate>=0.28          # device_map="auto" for multi-GPU
sentencepiece>=0.1.99     # tokenizer for Llama
huggingface_hub>=0.20     # model downloads

# ── Topology ───────────────────────────────────────────────────────
gudhi>=3.9                # Witness complex + persistent homology (primary)
scikit-learn>=1.3         # pairwise_distances for ε_max estimation

# Fallback topology (if gudhi fails)
ripser>=0.6
scikit-tda>=1.0

# ── Optional: Benchmark evaluation ─────────────────────────────────
# To measure actual GSM8K scores instead of using estimates:
#   lm-eval>=0.4
#   vllm>=0.4  (optional, faster inference)
